import type { Article } from "@/types/content";

/**
 * Guide articles (Article with type "guide"). Seeded in a later phase
 * (task T037, User Story 5). Empty in the V1 foundation.
 */
export const guides: Article[] = [];
