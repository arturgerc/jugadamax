import type { Article } from "@/types/content";

/**
 * News articles (Article with type "news"). Seeded in a later phase
 * (task T037, User Story 5). Empty in the V1 foundation.
 */
export const news: Article[] = [];
