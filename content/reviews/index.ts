import type { Review } from "@/types/content";

/**
 * Review records. Seeded in a later phase (task T022, User Story 2).
 * Empty in the V1 foundation; loaders handle an empty collection gracefully.
 */
export const reviews: Review[] = [];
