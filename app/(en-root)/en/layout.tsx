/**
 * Nested English segment layout.
 * Root chrome (html/body/header/footer) lives in app/(en-root)/layout.tsx.
 */
export default function EnSegmentLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
